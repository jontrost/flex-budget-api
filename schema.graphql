### This file was generated by Nexus Schema
### Do not make changes to this file directly


type Category {
  _id: ID!
  funds: [Fund!]
  name: String!
}

type Expense {
  _id: ID!
  cost: Float!
  date: String!
  name: String!
}

input ExpenseInput {
  cost: Float!
  date: String!
  name: String!
}

type Fund {
  _id: ID!
  budgetedAmount: Float!
  expenses: [Expense!]
  name: String!
}

input FundInput {
  budgetedAmount: Float!
  expenses: [ExpenseInput!]
  name: String!
}

type Mutation {
  createCategory(name: String!): Category
  createExpense(categoryId: ID!, cost: Float!, date: String!, fundId: ID!, name: String!): Category
  createFund(budgetedAmount: Float!, categoryId: ID!, name: String!): Category
  updateCategory(_id: ID!, funds: [FundInput!], name: String!): Category
  updateExpense(_id: ID!, categoryId: ID!, cost: Float!, date: String!, fundId: ID!, name: String!): Category
  updateFund(_id: ID!, budgetedAmount: Float!, currentCategoryId: ID!, name: String!, newCategoryId: ID!): Category
}

type Query {
  categories: [Category!]!
  category(_id: ID!): Category
  expense(_id: ID!): Expense
  expensesForFund(categoryId: ID!, fundId: ID!): [Expense!]!
  fund(_id: ID!, categoryId: ID!): Fund
}
